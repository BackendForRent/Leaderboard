# ===================================
#  Form Field Definitions
# ===================================

fields:
    name:
        label: NAME    
    order:
        label: ORDER    
        type: balloon-selector
        default: ascending
        options:
            ascending: ascending
            descending: descending    
